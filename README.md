# Flappie Bird:
Building a clone of flappy bird from scratch using python.
It was using one file, to simplify the project.

To rendering it uses a classic library of Python
called Pygame
For execute the game for itself, it is necessary execute this command below in case that you do not have
installed the library yet.

``pip install pygame``